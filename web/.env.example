# Required for correct metadata in production: the deployed site's absolute
# URL. Without it, og:image/twitter:image resolve to a relative path and
# link previews (Slack, iMessage, LinkedIn, etc.) break — they require an
# absolute image URL. Set as a Vercel project environment variable.
NEXT_PUBLIC_SITE_URL=https://city-maps-wabz.vercel.app
