# Optional: only needed for the PostGIS export path (livability.export.to_postgis).
# Not required for the default GeoJSON pipeline.
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
